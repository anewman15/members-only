<!DOCTYPE html>
<html>
  <head>
    <title>MembersOnly</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/774f0ae2db.js" crossorigin="anonymous"></script>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if flash[:notice] %>
    <div class="notification is-primary global-notification">
      <p class="notice"><%= notice %></p>
    </div>
    <% end %>
    <% if flash[:alert] %>
      <div class="notification is-danger global-notification">
        <p class="notification"> <%= alert %> </p>
      </div>
    <% end %>
    <nav class="navbar is-info">
      <div class="navbar-brand">
        <%= link_to root_path, class: "navbar-item" do %>
          <h1 class="title is-5">MembersOnly</h1>
        <% end %>      
        <div class="navbar-burger burger" data-target="navbarExample">
          <span></span>
          <span></span>
          <span></span>        
        </div>
      </div>
      <div id="navbarExample" class="navbar-menu">
        <div class="navbar-end mr-4">
          <div class="field is-grouped custom-centered">
            <p class="control">
              <%= link_to  "Insult Someone", root_path, class: "button is-info is-inverted has-text-centered" %>
            </p>
            <% if user_signed_in? %>
              <p class="control">
                <span class="button is-info"><strong>< <%= "#{current_user.fname} #{current_user.lname}" %>/strong></span>
              </p>
              <p class="control">
                <%= link_to  "Logout", destroy_user_session_path, method: :delete, class: "button is-info" %>
              </p>
            <% else %>
              <p class="control">
                <%= link_to  "Log In", new_user_session_path, class:"button is-info" %>
                </p>
              <p class="control">
                <%= link_to "Sign Up", new_user_registration_path, class: "button is-info" %>
              </p>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <%= yield %>
  </body>
</html>

